# 功能规格 — novel-writer-skills（Claude Code 专用小说创作工具）

**项目名**：novel-writer-skills
**形态**：Node.js CLI 工具（npm 包 `novel-writer-skills`，bin `novelwrite`）+ Claude Code 专用 Slash Commands 与 Agent Skills
**入口**：`src/cli.ts`（`novelwrite` 命令）；命令与技能内容位于 `templates/` 与生成后的 `.claude/` 目录

## 1. 系统架构与组件

| 组件 | 路径 | 形态 | 职责 |
|------|------|------|------|
| CLI 主程序 | `src/cli.ts` | TypeScript / Commander | 解析 `novelwrite` 子命令（init/check/plugin*/upgrade） |
| 版本 | `src/version.ts` | TypeScript | 版本号（1.1.1） |
| 插件管理 | `src/plugins/manager.ts` | TypeScript | `PluginManager`：安装/卸载/列表 |
| 工具 | `src/utils/{logger,project}.ts` | TypeScript | 日志、项目根定位与信息读写 |
| 命令模板 | `templates/commands/*.md` | Markdown | 15 个命令原文（constitution/specify/clarify/plan/tasks/write/analyze/checklist/track-init/track/plot-check/timeline/relations/world-check/expert） |
| 技能模板 | `templates/skills/` | Markdown/JSON | Agent Skills（三类，见 §4） |
| 知识库 | `templates/knowledge-base/` | Markdown | genres/requirements/styles 三类规则库 |
| 记忆/追踪/知识 | `templates/{memory,knowledge,tracking}/` | Markdown/JSON | 宪法模板、人物声音、追踪 JSON 模板、知识库 |
| 脚本版流程 | `templates/scripts/{bash,powershell}/` | Shell | 纯脚本七步流程 |
| 内置插件 | `plugins/{authentic-voice,book-analysis}/` | Markdown/YAML | 真实人声、拆书分析 |

## 2. CLI 命令（`src/cli.ts`）

`novelwrite init [name]` 复制链（已核实）：
- `templates/commands` → `.claude/commands`
- `templates/skills` → `.claude/skills`
- 整个 `templates/` → `.specify/templates`
- `templates/memory` → `.specify/memory`（及 `.claude/memory`）
- `templates/tracking` → `spec/tracking`
- `templates/knowledge` → `spec/knowledge`
- `templates/knowledge-base` → `.claude/knowledge-base`
- 可选 `--plugins`：经 `PluginManager` 安装 `plugins/<name>`（如 `authentic-voice`）
- 默认 `git init`（可 `--no-git`）

其余命令：
- `novelwrite check`：检测 Node.js / Git 安装状态（参考 `novel-writer` 实现）。
- `novelwrite plugin:list|add|remove <name>`：经 `PluginManager` 管理 `plugins/` 下内置插件（如 `authentic-voice`、`book-analysis`）。
- `novelwrite upgrade [--all | --commands | --skills | --knowledge-base]`：用包内最新文件覆盖项目对应目录（`overwrite: true`）。

## 3. Slash Commands（对应 Claude Code 斜杠命令）

内容为 `templates/commands/*.md` 复制到 `.claude/commands/`：

| 命令 | 输出文件 | 说明 |
|------|----------|------|
| `/constitution` | `.specify/memory/constitution.md` | 创作宪法：核心原则/价值观/禁忌/质量标准 |
| `/specify` | `stories/001-故事名/specification.md` | 故事规格（PRD 式） |
| `/clarify` | 更新 specification.md | 5 个问题澄清模糊点 |
| `/plan` | `stories/.../creative-plan.md` | 分卷/章节结构、技术方案 |
| `/tasks` | `stories/.../tasks.md` | 可执行任务清单 |
| `/write` | `stories/.../content/chapter-XX.md` | 基于任务清单创作 |
| `/analyze` | 分析报告 | 双模式（框架分析/内容分析）验证 |
| `/track-init` `/track` `/plot-check` `/timeline` `/relations` `/world-check` `/checklist` `/expert` | 追踪/知识库 | 一致性维护与专家模式 |

## 4. Agent Skills（Claude Code 自动激活）

| 系统 | 路径 | 子技能 |
|------|------|--------|
| 类型知识库 | `.claude/skills/genre-knowledge/` | `fantasy`（奇幻）、`mystery`（悬疑）、`romance`（言情） |
| 写作技巧 | `.claude/skills/writing-techniques/` | `dialogue-techniques`（对话）、`scene-structure`（场景结构） |
| 质量检查 | `.claude/skills/quality-assurance/` | `consistency-checker`、`forgotten-elements`、`getting-started`、`pre-write-checklist`、`requirement-detector`、`setting-detector`、`style-detector`、`workflow-guide` |

## 5. 知识库体系（`templates/knowledge-base/`）

| 类别 | 目录 | 内容 |
|------|------|------|
| 类型 | `genres/` | historical、mystery、revenge、romance、wuxia |
| 需求 | `requirements/` | anti-ai-v3、anti-ai-v4、fast-paced、no-poison、romance-angst、romance-sweet、serious-literature、strong-emotion |
| 风格 | `styles/` | ancient-style、literary、minimal、natural-voice、web-novel |

## 6. 关键文件结构（生成的项目）

```
my-novel/
├── .specify/            # Spec Kit 配置
│   ├── config.json
│   ├── memory/  templates/  scripts/{bash,powershell}/
├── .claude/             # Claude Code 专用
│   ├── commands/        # Slash Commands
│   ├── skills/          # Agent Skills（三类）
│   ├── knowledge-base/  # 类型/需求/风格规则库
│   ├── memory/  tracking/
├── spec/
│   ├── tracking/        # plot-tracker.json / timeline.json / character-state.json / relationships.json
│   └── knowledge/
└── stories/001-故事名/{specification,creative-plan,tasks,content/}
```

## 7. 约束与边界

- 纯流程编排工具，不内置大模型；内容由 Claude Code 生成。
- Agent Skills 为 Claude Code 独有体验；与 `novel-writer`（跨平台）共享七步方法论但分发形态不同。
- 与 `ai-novel-writer`（第一人称降重/8铁律）无耦合。
- 运行需 Node.js + Git（init 默认 git init）。
