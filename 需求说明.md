# 需求说明 — novel-writer-skills（Claude Code 专用小说创作工具）

**项目名**：novel-writer-skills（位于 `D:/WorkSpace/apps/Source/novel-writer-skills`）
**形态**：Node.js CLI 工具（npm 包 `novel-writer-skills`，bin `novelwrite`）+ Claude Code 专用 Slash Commands 与 Agent Skills
**主页**：https://github.com/wordflowlab/novel-writer-skills
**许可证**：MIT

## 1. 背景与问题

`novel-writer` 解决了「跨 13 个 AI 工具的七步方法论」，但仍有缺口：

- 作者若**只用 Claude Code**，不需要跨平台命令格式适配，反而需要 Claude Code 独有的 **Agent Skills**（知识库 + 自动检查系统）来获得「提到言情自动激活套路、后台自动监控一致性」的体验。
- 类型写作惯例（言情/悬疑/奇幻）、写作技巧（对话/场景/角色弧）、质量检查（一致性/遗忘元素/伏笔）等**领域知识需要按上下文自动注入**，而非每次手动查阅。
- 用户希望 `novelwrite init` 一键把命令、技能、知识库、追踪模板、插件全布置好。

## 2. 目标用户

- 在 **Claude Code** 中创作中文小说的作者，追求「最佳体验 + 自动智能辅助」。
- 需要类型知识库自动激活、质量检查后台自动运行的作者。

## 3. 核心目标

在 `novel-writer` 七步方法论基础上，**专为 Claude Code 深度增强**：提供 Slash Commands（用户主动调用）+ Agent Skills（AI 自动激活的知识库与检查系统）+ 知识库体系 + 插件机制，使 `novelwrite init` 一键生成完整项目。

## 4. 功能需求

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-1 | 安装 | `npm install -g novel-writer-skills`；支持 Windows / macOS / Linux |
| FR-2 | 初始化项目 | `novelwrite init <项目名>`：生成 `.specify/`（memory/templates/scripts）、`.claude/`（commands/skills/knowledge-base/memory/tracking），写 `.specify/config.json`，复制命令/技能/知识库/追踪模板，可选 `--here` / `--plugins` / `--all` / `--no-git` |
| FR-3 | Slash Commands | 七步方法论命令：`/constitution` `/specify` `/clarify` `/plan` `/tasks` `/write` `/analyze`，及追踪命令 `/track-init` `/track` `/plot-check` `/timeline` `/relations` `/world-check` `/checklist` `/expert`（`templates/commands/*.md`） |
| FR-4 | Agent Skills（类型知识库） | `.claude/skills/genre-knowledge/`：言情(romance)/悬疑(mystery)/奇幻(fantasy)，提到对应类型自动激活 |
| FR-5 | Agent Skills（写作技巧） | `.claude/skills/writing-techniques/`：对话技巧(dialogue-techniques)/场景结构(scene-structure)，写作时自动应用 |
| FR-6 | Agent Skills（质量检查） | `.claude/skills/quality-assurance/`：一致性检查(consistency-checker)、遗忘元素(forgotten-elements)、写前清单(pre-write-checklist)、需求检测(requirement-detector)、设定检测(setting-detector)、风格检测(style-detector)、流程引导(workflow-guide)，后台自动监控 |
| FR-7 | 知识库体系 | `templates/knowledge-base/`：类型(genres: historical/mystery/revenge/romance/wuxia)、需求(requirements: anti-ai-v3/v4、fast-paced、no-poison、romance-angst/sweet、serious-literature、strong-emotion)、风格(styles: ancient-style/literary/minimal/natural-voice/web-novel) |
| FR-8 | 插件系统 | `novelwrite plugin:list/add/remove <name>`；内置 `authentic-voice`（真实人声）、`book-analysis`（拆书分析）等 |
| FR-9 | 升级 | `novelwrite upgrade [--all \| --commands \| --skills \| --knowledge-base]` 覆盖项目旧文件 |
| FR-10 | 环境检查 | `novelwrite check` 校验 Node.js / Git / Claude Code |
| FR-11 | 脚本版流程 | `templates/scripts/{bash,powershell}/` 提供不依赖 Claude Code 的纯脚本七步流程（含 `check-world.sh` 等） |
| FR-12 | 多线索管理 | 支持主线 + 副线（>20 章未推进副线可提醒）的线索追踪 |

## 5. 非功能需求

- **Claude Code 最佳体验**：Agent Skills 自动激活，无需手动调用类型/技巧知识库。
- **可恢复/可追踪**：状态真相源是 Markdown 文件与追踪 JSON，非数据库；重启可无损恢复。
- **可扩展**：插件 + 知识库 + 技能三层扩展机制。
- **零依赖 AI 推理**：工具只编排流程与文件，内容由 Claude Code 生成。

## 6. 范围与边界

- **范围内**：Claude Code 专用 Slash Commands、Agent Skills（三类）、知识库体系、插件系统、七步方法论。
- **范围外**：不内置大模型推理；不负责发布、降重、AI 率检测（由 `ai-novel-writer` / `fanqie_auto_publish` 承担）。
- 技术栈：TypeScript + Commander.js CLI（bin `novelwrite`）；运行时需 Node.js 与 Git。
- 与 `novel-writer`（跨 13 平台）共享方法论但分发形态不同；与 `ai-novel-writer`（第一人称降重/8铁律）无耦合。
